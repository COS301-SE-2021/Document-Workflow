name: Ionic CI
on:
  pull_request:
    paths:
      - 'app/**'
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: yarn run build:app
      - run: yarn run test:app
